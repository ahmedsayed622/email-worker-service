create or replace procedure CMP_CLIENTS_PRO_DOB as 
v_rowcount NUMBER;
begin

--date_of_birth
--CMP_CLIENTS_TBL_DOB
--INSERT INTO CMP_EMP_TBL_DAILY_ORDERS (PROFILE_ID, CUSTOMER_NAME_EN, INVOICE_DATE, INVOICE_NO, EXECID, STOCK_ID, QUNTY, SECOND_PROFILE)
--
--select C.PROFILE_ID , C.CUSTOMER_NAME_EN , A.INVOICE_DATE , A.INVOICE_NO, SUBSTR(b.EXECID ,14,18) as "EXECID",A.STOCK_ID, B.QUNTY ,
--(select F.PROFILE_ID
--from BO_INVOICE_HEADER f
--inner join BO_INVOICE_DETAIL g on f.INVOICE_TRNX_ID = g.INVOICE_TRNX_ID where f.OPERATION_ID <> a.OPERATION_ID and SUBSTR(g.EXECID ,14,18) = SUBSTR(B.EXECID ,14,18) ) as "SECOND_PROFILE"
--from BO_INVOICE_HEADER a
--inner join BO_INVOICE_DETAIL b on A.INVOICE_TRNX_ID = B.INVOICE_TRNX_ID
--inner join T_PROFILE_INFO_SETTING c on A.PROFILE_ID = c.PROFILE_ID
--where A.INVOICE_DATE = Edata_Getdate(SYSDATE) AND C.ISEMPLOYEE = 1 and SUBSTR(B.EXECID ,14,18) in 
--(select SUBSTR(e.EXECID ,14,18)	
--from BO_INVOICE_HEADER d
--inner join BO_INVOICE_DETAIL e on d.INVOICE_TRNX_ID = e.INVOICE_TRNX_ID where D.OPERATION_ID <> a.OPERATION_ID  )
--order by A.INVOICE_DATE;

INSERT INTO CMP_CLIENTS_TBL_CTRL_DOB 
VALUES ( TO_NUMBER ( TO_CHAR(SYSDATE,'YYYYMMDD') )) ;

INSERT INTO CMP_CLIENTS_TBL_DOB (
PROFILE_ID, CUSTOMER_NAME_EN, CUSTOMER_NAME_AR, NIN, 
C_ACCOUNT, MOBILE_1, MOBILE_2, CLIENT_SUSPEND, NOTES, DOB, DATE_OF_BIRTH, FLAG , INSERT_DATE
)

select distinct A.PROFILE_ID, A.CUSTOMER_NAME_EN, A.CUSTOMER_NAME_AR , B.NIN , B.C_ACCOUNT,A.MOBILE_1,A.MOBILE_2,case when ALL_TO_DATE > 20250403 then 'Yes' else 'No' end as client_SUSPEND, C.NOTES, A.DOB,
EDATA_CAST_DATETIME ( A.DOB , 'D,/,M,/,Y') as date_of_birth ,1 , TO_NUMBER ( TO_CHAR(SYSDATE,'YYYYMMDD') )
from T_PROFILE_INFO_SETTING A
inner join T_INVESTMENT_ACCOUNT_PROFILE B ON A.PROFILE_ID = B.PROFILE_ID 
left join BO_CLIENT_SUSPEND c on A.PROFILE_ID = C.PROFILE_ID
where A.PROFILE_ID = B.PROFILE_ID and substr(A.DOB,5,4) between 0401 and 0431 and A.CUSTOMER_CATEGORY =1
order by date_of_birth;


-- Check if no rows were inserted
v_rowcount := SQL%ROWCOUNT;

IF v_rowcount = 0 THEN
  -- No data found, insert placeholder record with today's date and flag = 0
  INSERT INTO CMP_CLIENTS_TBL_DOB ( FLAG)
  VALUES ( 0);
END IF;

COMMIT;




  null;
end CMP_CLIENTS_PRO_DOB;
