/**
 * Operations domain hooks.
 * processData: Called after fetching rows, before export.
 */
export default {
  async processData(rows, closeDate) {
    // Placeholder: custom operations processing
    return rows;
  },
};
